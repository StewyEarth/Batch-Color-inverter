# Features
- Batch invert image colors
- Crop images with adjustable selection
- Adjust blackpoint and whitepoint per image
- Download all processed images at once
- Optionally rename files in numeric order
- Optionally use a custom filename prefix
- Preview and navigate images in a modal
- Modern, responsive UI
- All processing is local (no uploads)